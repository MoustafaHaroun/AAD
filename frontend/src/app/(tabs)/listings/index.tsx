import ListingsScreen from "@/presentation/screens/listings.screen";
import * as React from "react";

/**
 * Render the Listings screen route.
 * @returns The rendered Listings screen.
 */
export default function Screen(): React.JSX.Element {
    return <ListingsScreen />;
}
