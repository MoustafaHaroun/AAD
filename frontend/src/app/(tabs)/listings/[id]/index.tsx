import ListingScreen from "@/presentation/screens/listing.screen";
import * as React from "react";

/**
 * Render the Listing detail screen route.
 * @returns The rendered Listing detail screen.
 */
export default function Screen(): React.JSX.Element {
    return <ListingScreen />;
}
