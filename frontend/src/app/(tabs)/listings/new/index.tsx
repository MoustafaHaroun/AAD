import CreateListingScreen from "@/presentation/screens/create-listing.screen";
import * as React from "react";

/**
 * Render the Create Listing screen route.
 * @returns The rendered Create Listing screen.
 */
export default function Screen(): React.JSX.Element {
    return <CreateListingScreen />;
}
