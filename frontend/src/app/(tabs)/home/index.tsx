import HomeScreen from "@/presentation/screens/home.screen";
import * as React from "react";

/**
 * Render the Home screen route.
 * @returns The rendered Home screen.
 */
export default function Screen(): React.JSX.Element {
    return <HomeScreen />;
}
