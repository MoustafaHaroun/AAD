import PreferencesScreen from "@/presentation/screens/preferences.screen";
import * as React from "react";

/**
 * Render the Preferences screen route.
 * @returns The rendered Preferences screen.
 */
export default function Screen(): React.JSX.Element {
    return <PreferencesScreen />;
}
