import AccountScreen from "@/presentation/screens/account.screen";
import * as React from "react";

/**
 * Render the Account screen route.
 * @returns The rendered Account screen.
 */
export default function Screen(): React.JSX.Element {
    return <AccountScreen />;
}
