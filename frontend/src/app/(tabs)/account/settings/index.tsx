import AccountSettingsScreen from "@/presentation/screens/account-settings.screen";
import * as React from "react";

/**
 * Render the Account Settings screen route.
 * @returns The rendered Account Settings screen.
 */
export default function Screen(): React.JSX.Element {
    return <AccountSettingsScreen />;
}
