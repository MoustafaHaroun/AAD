import ChatsScreen from "@/presentation/screens/chats.screen";
import * as React from "react";

/**
 * Render the Chats list screen route.
 * @returns The rendered Chats screen.
 */
export default function Screen(): React.JSX.Element {
    return <ChatsScreen />;
}
