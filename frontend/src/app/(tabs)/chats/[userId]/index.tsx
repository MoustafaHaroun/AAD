import ChatScreen from "@/presentation/screens/chat.screen";
import * as React from "react";

/**
 * Render the Chat screen route for a single conversation.
 * @returns The rendered Chat screen.
 */
export default function Screen(): React.JSX.Element {
    return <ChatScreen />;
}
